"use strict" // treat all JS code as newer version
//alert("Hello")// we are using node js ,not browser so avoid to write alert like this.
 let name = "Armaan"
 let age = 25;
 let isStudying = true;
 //number => 2 to the power 53
 //bigint
 //String => " "
 //boolean => true/false
 // null => standalone value but it is one of the datatype 
 // undefined => 
    //symbol => unique
 console.log(name);
 console.log(typeof(name));
 console.log(typeof(null));
 console.log(typeof(undefined));
 console.log(typeof(isStudying));
